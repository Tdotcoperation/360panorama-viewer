const BLOCKED_HOST=/^(localhost|0\.0\.0\.0|127\.|10\.|192\.168\.|169\.254\.|172\.(1[6-9]|2\d|3[01])\.|::1$|fc|fd)/i;export default{async fetch(request,env){const u=new URL(request.url);if(u.pathname!='/proxy')return env.ASSETS.fetch(request);const raw=u.searchParams.get('url');if(!raw)return new Response('Missing url',{status:400});let target;try{target=new URL(raw)}catch{return new Response('Invalid url',{status:400})}if(!['http:','https:'].includes(target.protocol)||BLOCKED_HOST.test(target.hostname)||target.hostname.endsWith('.local'))return new Response('Blocked target',{status:403});try{const r=await fetch(target.toString(),{redirect:'follow',headers:{'User-Agent':'360PanoramaViewer/1.0','Accept':'image/*,*/*;q=.5'}});if(!r.ok)return new Response('Upstream error',{status:502});const len=Number(r.headers.get('content-length')||0);if(len>50*1024*1024)return new Response('Image too large',{status:413});const h=new Headers();h.set('Content-Type',r.headers.get('content-type')||'application/octet-stream');h.set('Cache-Control','public, max-age=3600');h.set('Access-Control-Allow-Origin','*');h.set('X-Content-Type-Options','nosniff');return new Response(r.body,{status:200,headers:h})}catch{return new Response('Proxy fetch failed',{status:502})}}}}